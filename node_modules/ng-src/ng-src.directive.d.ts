import { OnChanges, ViewContainerRef, EventEmitter } from '@angular/core';
export declare class NgSrcDirective implements OnChanges {
    private view;
    ngSrc: string;
    ngOnLoad: EventEmitter<XMLHttpRequest>;
    readonly hostImage: HTMLImageElement;
    constructor(view: ViewContainerRef);
    ngOnChanges(): void;
    private fetchImage(url);
    private onRequestLoaded(ev);
    private createLocalResource(request);
    private setImageSource(url);
    private getContentType(request);
}
